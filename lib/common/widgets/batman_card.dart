import 'package:flutter/material.dart';

class BatmanCard extends StatelessWidget {
  const BatmanCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
