import 'package:flutter/material.dart';

class MatchSquads extends StatelessWidget {
  const MatchSquads({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
